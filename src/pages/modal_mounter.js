import React from "react";

const ModalMounter = ({ location: { search }}) => {
  // const episodeId = search.split('?episode=')[1];
  // if (episodeId) return(
  //   <RekModal episodeId={episodeId} isOpen={true} />
  // );
  return(
    <div></div>
  )
}

export default ModalMounter;
